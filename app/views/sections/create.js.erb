<% if @section.errors.any? %>
  $(".error_explanation").html("<%= escape_javascript(render('error'))%>")
<% else %>
  $("#new-section-modal").modal('hide')
  $("#section-index").append("<%= escape_javascript(render('section', section: @section )) %>")
<% end %>